function lookForUpdades() {}

const interval_30_minutes = 60 * 30 * 1000;

setInterval(lookForUpdades, interval_30_minutes); // 30 minutes

function calculateDiscount(priceInCents, discountAmountInPercentage) {}
